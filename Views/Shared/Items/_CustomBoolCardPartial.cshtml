@model CatalogR.Models.CustomFieldValues<bool>
@if (Model.State)
{
    <div class="form-check">
        <input class="form-check-input" type="checkbox" asp-for="Value" disabled>
        <label class="control-label">
            @Model.Name
        </label>
    </div>
}